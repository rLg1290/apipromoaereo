version: "3.8"

services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # persiste o banco SQLite entre restarts
    environment:
      DB_PATH: /app/data/promotions.db
